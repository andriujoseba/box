### Changed

- `box new` now accepts tenant roles and named sizes without per-agent templates, so a new rig role needs no box release (#159).
- Retired agent templates now point callers to `--role <name> --size medium`; blank mints omit `--template` (#159).
