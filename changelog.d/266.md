### Fixed

- An agent timeout keeps a forensic bundle — console, host-side instance log, expanded config, state — says whether an empty console went unread or the guest was silent, stops waiting on a guest that is no longer running, and no longer exits inside its own diagnostic (#266).
- The forensic bundle is a fresh private directory per attempt, never a shared `/tmp` name another tenant could have prepared, and box names only the captures it actually stored (#266).
