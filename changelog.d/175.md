### Fixed

- Every template now requires the VM trust boundary by default, so a host without KVM fails loudly instead of silently minting a weaker container; an explicit `--container` remains the deliberate escape hatch (#175).
