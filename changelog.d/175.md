### Fixed

- Agent and blank templates now declare `BOX_NO_CONTAINER_FALLBACK`, so KVM-less hosts fail instead of silently minting a container while an explicit `--container` remains available (#175).
